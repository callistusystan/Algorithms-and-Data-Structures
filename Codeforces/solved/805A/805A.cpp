#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef vector<int> vi;

int main() {
  ios::sync_with_stdio(0); cin.tie(0);

  int L,R; cin >> L >> R;
  if (L == R) cout << L << endl;
  else cout << 2 << endl;

  return 0;
}
